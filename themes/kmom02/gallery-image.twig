{% include 'incl/header.twig' %}

<body>
    {% include 'incl/nav.twig' %}

    <div class="main" role="main">
        <div class="container report">
            {% set file = url_param('file', 'string') %}
            {% if file %}
                <img src="{{ base_url }}/image/{{ file }}" alt="{{ file }}">
            {% else %}
                <p>Ingen bild add visa</p>
            {% endif %}
        </div>
    </div>

    {% include 'incl/footer.twig' %}
</body>

</html>
