<!DOCTYPE html>
<html class="no-js" lang="en">

<head>
    <meta charset="utf-8" >
    <meta name="viewport" content="width=device-width, initial-scale=1" >
    <title>{% if meta.title %}{{ meta.title }} | {% endif %}{{ site_title }}</title>

    {% if meta.description %}
    <meta name="description" content="{{ meta.description|striptags }}" >
    {% endif %}

    {% if current_page %}
    <link rel="canonical" href="{{ current_page.url }}" >
    {% endif %}

    <link rel="icon" href="{{ theme_url }}/../shared/img/favicon.ico">

    {% if config.session.theme == "dark" %}
        <link rel="stylesheet" href="{{ theme_url }}/css/style-dark.css" type="text/css" media="screen" />
    {% else %}
        <link rel="stylesheet" href="{{ theme_url }}/css/style.css" type="text/css" media="screen" />
    {% endif %}

</head>
